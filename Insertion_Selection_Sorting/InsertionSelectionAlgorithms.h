#ifndef SORTLIBRARY_H_INCLUDED
#define SORTLIBRARY_H_INCLUDED
#endif

#include <iostream>
#include <fstream>
#include <time.h>
#include <vector>
#include "../Results.h"

Results InsertionSort(std::vector<int> &vet);
void SelectionSort(std::vector<int> &vet);